{
  "private": true,
  "name": "git-guardrails-hooks",
  "description": "Build tooling for guard-gh-write Node.js hook",
  "scripts": {
    "build": "esbuild hooks/guard-gh-write.js --bundle --platform=node --target=node18 --outfile=hooks/guard-gh-write.bundle.js"
  },
  "devDependencies": {
    "bash-parser": "^0.5.0",
    "esbuild": "^0.24.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["esbuild"]
  }
}
